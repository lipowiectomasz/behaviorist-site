services:
  webserver:
    container_name: behawiorysta
    image: 'nginx:alpine'
    working_dir: /var/www/html
    ports:
      - '8007:80'
    volumes:
      - .:/var/www/html
      - '.docker/nginx.conf:/etc/nginx/conf.d/default.conf'